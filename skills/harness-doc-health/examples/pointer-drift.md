# Pointer Drift Example

A common failure mode:

- `PROGRESS.md` says the team is validating delivery
- `NEXT_STEP.md` still says to write the first spec
- `AGENT_INDEX.md` routes to a retired skill

This is already drift, even if no code changed. Fix the pointers first so the repository becomes legible again.
